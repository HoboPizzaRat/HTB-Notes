
```
HoboPizzaRat@htb[/htb]$ ### Syntax: src/dst [host|net|port] [IP|Network Range|Port]
HoboPizzaRat@htb[/htb]$ sudo tcpdump -i eth0 src host 172.16.146.2
```

Source and destination allow us to work with the directions of communication. For example, in the last output, we have specified that our `source` host is `172.16.146.2`, and only packets sent from this host will be intercepted.
