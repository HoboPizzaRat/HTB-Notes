```
HoboPizzaRat@htb[/htb]$ ### Syntax: less/greater [size in bytes]
HoboPizzaRat@htb[/htb]$ sudo tcpdump -i eth0 less 64
```
Above was an excellent example of using `less`. We can utilize the modifier `greater 500` to only show me packets with 500 or more bytes.
