# HTTP Requests
![[Pasted image 20260816172224.png]]

|   |   |   |
|---|---|---|
|`Method`|`GET`|The HTTP method or verb, which specifies the type of action to perform.|
|`Path`|`/users/login.html`|The path to the resource being accessed. This field can also be suffixed with a query string (e.g. `?username=user`).|
|`Version`|`HTTP/1.1`|The third and final field is used to denote the HTTP version.|

# HTTP Response
![[Pasted image 20260816172503.png]]
For example we can get the session cookies, content type definition, the content itself etc on the response


## CURL with https
cURL also allows us to preview the full HTTP request and the full HTTP response, which can become very handy when performing web penetration tests or writing exploits.
